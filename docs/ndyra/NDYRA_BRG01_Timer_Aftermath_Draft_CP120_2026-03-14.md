# NDYRA BRG01 Timer ↔ Aftermath Draft - CP120

## Purpose
Define one stable session payload between Timer and the social recap layer.

## Why Core owns the draft
Timer still owns runtime, replay, and fork mechanics. Social still owns Aftermath and story rendering. Core owns the bridge contract so the two sides can hand back cleanly later.

## Scope
This checkpoint only adds the draft contract and Core shell readiness. It does not merge Timer runtime.
