# NDYRA Inbox Message Center Polish — CP107

This checkpoint upgrades the existing Inbox into a more messenger-style surface: search, unread-only filter, and direct-thread actions (archive/clear). Live mode uses existing DM RPCs; local preview uses deterministic seed data with actions disabled.
